{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "yarn generate && touch .output/public/.nojekyll",
    "deploy": "yarn predeploy && gh-pages -d dist -t true"
  },
  "devDependencies": {
    "@invictus.codes/nuxt-vuetify": "^0.2.20",
    "@types/node": "^18",
    "gh-pages": "^5.0.0",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "sass": "^1.62.1"
  }
}
